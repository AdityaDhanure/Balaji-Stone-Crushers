import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Brand Colors
  static const Color primary = Color(0xFF1E3A5F);        // Deep navy blue
  static const Color primaryLight = Color(0xFF2E5D9F);   // lighter blue
  static const Color primaryDark = Color(0xFF0F1E32);    // darker blue

  // Accent
  static const Color accent = Color(0xFFE67E22);         // Orange (crusher theme)
  static const Color accentLight = Color(0xFFF39C12);    // Light orange

  // Background
  static const Color background = Color(0xFFF4F6F9);     // Light grey bg
  static const Color surface = Color(0xFFFFFFFF);        // White cards
  static const Color sidebarBg = Color(0xFF1E3A5F);      // Sidebar bg

  // Text
  static const Color textPrimary = Color(0xFF1A1A2E);    // Dark text
  static const Color textSecondary = Color(0xFF6B7280);  // Grey text
  static const Color textLight = Color(0xFFFFFFFF);      // White text

  // Status Colors
  static const Color success = Color(0xFF27AE60);        // Green
  static const Color warning = Color(0xFFF39C12);        // Yellow
  static const Color error = Color(0xFFE74C3C);          // Red
  static const Color info = Color(0xFF3498DB);           // Blue

  // Sidebar
  static const Color sidebarText = Color(0xFFB0BEC5);
  static const Color sidebarActive = Color(0xFFFFFFFF);
  static const Color sidebarActiveBg = Color(0xFF2E5D9F);
  static const Color sidebarHover = Color(0xFF243F6B);

  // Border & Divider
  static const Color border = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFF3F4F6);

  // Card Shadow
  static const Color shadow = Color(0x1A000000);
}